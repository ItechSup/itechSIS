{% extends 'ItechSupItechSisBundle::layout.html.twig' %}

{% block content %}
    <h1>Aperçu de la formation</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Titre</th>
                <td class="td-table">{{ entity.title }}</td>
            </tr>
            <tr>
                <th>Action</th>
                <td>
                    <a href="{{ path('formation_edit', { 'id': entity.id }) }}"class="button">Editer</a>
                </td>
            </tr>
        </tbody>
    </table>
    {{form_widget(delete_form,{'attr': {'class': 'new-entry button'}}) }}
    <br>
    <br>
    <br>
    <br>
    <br>
    <a href="{{ path('formation') }}" class="new-entry button">
        Retour à la liste des formations
    </a>

{% endblock %}
