{% extends 'ItechSupItechSisBundle::layout.html.twig' %}

{% block title %}Salle {{ entity.number }}{% endblock %}

{% block content %}

    <table class="table">
        <tbody>
            <tr>
                <th>Nombre de sièges</th>
                <td>{{ entity.seatsCount }}</td>
            </tr>
            <tr>
                <th>Nombre d'ordinateurs</th>
                <td>{{ entity.computersCount }}</td>
            </tr>
        </tbody>
    </table>

    {{ form_start(delete_form) }}
    <a href="{{ path('room_edit', { 'id': entity.id }) }}" class="btn btn-default">Editer</a>
    <button type="submit" class="btn btn-default">Supprimer</button>
    <a href="{{ path('room') }}" class="btn btn-default">Retour</a>
    {{ form_end(delete_form) }}

    {% include 'ItechSupItechSisBundle::calendar.html.twig' with {'events': entity.events } %}
{% endblock %}
