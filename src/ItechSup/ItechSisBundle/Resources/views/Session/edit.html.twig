{% extends 'ItechSupItechSisBundle::layout.html.twig' %}

{% block content%}
    <h1 class="block-title">Edition des sessions</h1>

    {{ form(edit_form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('session') }}">
                Retour à la liste
            </a>
        </li>

        <li>{% if entity.formation == "" %}
                {{ form(delete_form) }}
            {% endif %}
        </li>
    </ul>
{% endblock %}
