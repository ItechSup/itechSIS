{% extends 'ItechSupItechSisBundle::layout.html.twig' %}

{% block content %}
    <h1>Profs</h1>

    <div id="form">

        {{ form_start(edit_form)}}
        {{ form_errors(edit_form) }}
        <div class="widget form-title">
            <div class="label">
                <label>Nom complet du professeur</label>
            </div>
            {{ form_widget(edit_form.fullName, {'attr': {'class':'form-label'}}) }}
        </div>
        <div class="widget form-submit">
            {{ form_widget(edit_form.submit, {'attr': {'class': 'button button'}}) }}
        </div>
        <hr>
        {{ form_end(edit_form)}}
    </div>

        <ul class="record_actions">
    <li>
        <a href="{{ path('teacher') }}"class="new-entry button">
            Retour à la liste des profs
        </a>
    </li>
    <li>{{ form(delete_form, {'attr': {'class': 'new-entry button'}}) }}</li>
</ul>
{% endblock %}
